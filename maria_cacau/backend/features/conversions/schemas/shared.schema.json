{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$defs": {
        "FieldStatus": {
            "description": "Presença de um campo usado no user_data da Meta. Sem rótulo de exibição de propósito — é a tela quem decide o texto, via assets/strings.py.",
            "type": "object",
            "required": ["key", "present"],
            "properties": {
                "key":     { "type": "string", "enum": ["phone", "email", "name", "zip", "city_state"] },
                "present": { "type": "boolean" },
                "value":   { "type": ["string", "null"], "description": "Valor real, só quando present == true" }
            }
        }
    }
}
