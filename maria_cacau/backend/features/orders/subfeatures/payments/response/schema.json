{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Payments Pendent Response",
    "description": "Resposta de GET /orders/payments-pendent — envelope com o total e os pedidos da data com pagamento pendente.",
    "type": "object",
    "required": ["total", "orders"],
    "properties": {
        "total": {
            "type": "integer",
            "description": "Total de pedidos com pagamento pendente"
        },
        "orders": {
            "type": "array",
            "items": { "$ref": "../../../../../shared/schemas/shared.schema.json#/$defs/Order" }
        }
    }
}
